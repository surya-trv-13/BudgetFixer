import React from "react";
import { withStyles } from "@material-ui/styles";
import { Box } from "@material-ui/core";

const styles = (theme) => ({});

function HomeHeader() {
	return <Box>HomeHeader</Box>;
}

export default withStyles(styles, { withTheme: true })(HomeHeader);
